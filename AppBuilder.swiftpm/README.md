# App Builder

This folder is the complete Swift Playgrounds app. Open **AppBuilder.swiftpm** in Swift Playgrounds on iPad.

The package is dependency-free. All app source files are next to `Package.swift`, which is the layout Swift Playgrounds uses for app projects.
